---
import { getEntry, getCollection } from 'astro:content';
import { tenChinh, nhan } from '../lib/quy-cach';

/**
 * Header 2 tang, hoc cau truc baogiathep.net (do 01/09/2026):
 *   tang 1 = top bar do co email + hotline
 *   tang 2 = logo + nav + mega menu + nut CTA
 *
 * MEGA MENU LA THIET BI SEO, khong chi la dieu huong. baogiathep dat 99 link
 * trong nav va nav hien tren MOI trang -> moi trang tien chi cach nhau 1 cu nhay
 * tu bat ky dau, crawler vao trang nao cung thay het.
 *
 * KHAC HO O HAI CHO:
 *  1. Chi link toi trang DA TON TAI. Nhom nao chua tach trang quy cach thi chi
 *     link trang nhom - link toi trang chua co la tu tao 404.
 *  2. Mobile VAN CO nav day du (hamburger). baogiathep an han nav duoi 900px,
 *     tuc cat het internal link o thiet bi chiem phan lon traffic.
 */
const ct = await getEntry('congTy', 'cong-ty');
const c = ct!.data;

const nhom = (await getCollection('nhomSanPham')).sort((a, b) =>
  a.data.tachTrangQuyCach === b.data.tachTrangQuyCach ? 0 : a.data.tachTrangQuyCach ? -1 : 1,
);

const duongDanHienTai = Astro.url.pathname;
const dangO = (d: string) => duongDanHienTai === d || duongDanHienTai.startsWith(d + '/');

const navChinh = [
  { ten: 'Bảng giá', duongDan: '/bang-gia-dinh-thep/' },
  { ten: 'Bảng tra quy cách', duongDan: '/bang-tra-quy-cach-dinh/' },
  { ten: 'Nhà máy', duongDan: '/nha-may/' },
  { ten: 'Kiến thức', duongDan: '/kien-thuc/' },
  { ten: 'Liên hệ', duongDan: '/lien-he/' },
];
---

<header class="dau-trang">
  <div class="thanh-tren">
    <div class="khung thanh-tren__trong">
      <span class="thanh-tren__muc">
        <a href={`mailto:${c.email}`}>{c.email}</a>
      </span>
      <span class="thanh-tren__muc thanh-tren__muc--manh">
        <a href={`tel:${c.hotlineTel}`}>Hotline {c.hotline}</a>
      </span>
      <span class="thanh-tren__xh an-mobile">
        <a href={`tel:${c.hotlineTel}`} aria-label="Gọi hotline">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6.5 3.5 4 4.2c-1 .3-1.6 1.3-1.3 2.3C4 12 9 17 14.5 18.3c1 .3 2-.3 2.3-1.3l.7-2.5-3.6-1.6-1.3 1.6a12 12 0 0 1-4.4-4.4l1.6-1.3-1.6-3.6Z"></path></svg>
        </a>
        <a href={`https://zalo.me/${c.zalo}`} rel="noopener" aria-label="Nhắn Zalo">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 5.5A1.5 1.5 0 0 1 5.5 4h13A1.5 1.5 0 0 1 20 5.5v9a1.5 1.5 0 0 1-1.5 1.5H9l-4 4v-4H5.5A1.5 1.5 0 0 1 4 14.5v-9Z"></path></svg>
        </a>
        <a href={`mailto:${c.email}`} aria-label="Gửi email">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6.5A1.5 1.5 0 0 1 4.5 5h15A1.5 1.5 0 0 1 21 6.5v11a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 3 17.5v-11Z"></path><path d="m3.6 6.2 8.4 6 8.4-6"></path></svg>
        </a>
        <a href={c.websiteMe} rel="noopener" aria-label="Website công ty">
          <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><path d="M3 12h18M12 3a15 15 0 0 1 0 18a15 15 0 0 1 0-18"></path></svg>
        </a>
      </span>
    </div>
  </div>

  <div class="khung dau-trang__trong">
    <a class="hieu" href="/" aria-label={`${c.tenThuongHieu} - trang chủ`}>
      <span class="hieu__ten">ĐINH THÉP SÀI GÒN</span>
      <span class="hieu__phu">Nhà máy sản xuất - Cường Phát Steel</span>
    </a>

    <button
      class="nut-menu"
      type="button"
      aria-label="Mở menu"
      aria-expanded="false"
      aria-controls="dieu-huong"
    >
      <span></span><span></span><span></span>
    </button>

    <nav id="dieu-huong" class="dieu-huong" aria-label="Điều hướng chính">
      <ul class="dieu-huong__cap1">
        <li class="co-mega">
          <a href="/san-pham/" aria-expanded="false" aria-haspopup="true">Sản phẩm</a>

          <div class="mega">
            <div class="khung mega__trong">
              {
                nhom.map((n) => (
                  <div class="mega__cot">
                    <a class="mega__dau" href={`/san-pham/${n.data.slug}/`}>
                      {n.data.ten}
                    </a>
                    {n.data.tachTrangQuyCach ? (
                      <ul class="mega__ds">
                        {n.data.quyCach.map((q) => (
                          <li>
                            <a href={`/san-pham/${q.slug}/`}>
                              {tenChinh(n.data.ten, q)} <span class="mega__mm">{nhan(q)}</span>
                            </a>
                          </li>
                        ))}
                      </ul>
                    ) : (
                      <p class="mega__mota">{n.data.moTaNgan}</p>
                    )}
                  </div>
                ))
              }
              <div class="mega__cot mega__cot--nhan">
                <span class="mega__dau">Tra cứu</span>
                <ul class="mega__ds">
                  <li><a href="/bang-gia-dinh-thep/">Bảng giá đầy đủ quy cách</a></li>
                  <li><a href="/bang-tra-quy-cach-dinh/">Bảng tra phân - cm - mm</a></li>
                  <li><a href="/bang-gia-dinh-thep/#bu-long">Bảng tra bu lông M3-M36</a></li>
                </ul>
              </div>
            </div>
          </div>
        </li>

        {
          navChinh.map((m) => (
            <li>
              <a href={m.duongDan} aria-current={dangO(m.duongDan) ? 'page' : undefined}>
                {m.ten}
              </a>
            </li>
          ))
        }
      </ul>

      <a class="nut nut--chinh dieu-huong__cta" href={`tel:${c.hotlineTel}`}>
        Gọi báo giá
      </a>
    </nav>
  </div>
</header>

<style>
  .dau-trang {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--trang);
    border-bottom: var(--vien-mong);
  }

  /* Tang 1: dai do. Do la #C4161C - MOT do duy nhat, khac ho dung hai. */
  .thanh-tren {
    background: var(--do-500);
    color: var(--trang);
    font-size: var(--cs-micro);
    font-weight: var(--w-vua);
  }
  .thanh-tren__trong {
    display: flex;
    align-items: center;
    gap: var(--k5);
    min-height: 34px;
    flex-wrap: wrap;
  }
  .thanh-tren__muc a {
    color: var(--trang);
    text-decoration: none;
  }
  .thanh-tren__muc a:hover {
    text-decoration: underline;
  }
  .thanh-tren__muc--manh {
    font-weight: var(--w-nang);
  }
  .thanh-tren__muc:last-child,
  .thanh-tren__xh {
    margin-left: auto;
  }
  /* Icon xa hoi ben phai top bar - giong bo cuc top bar cua baogiathep */
  .thanh-tren__xh {
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .thanh-tren__xh a {
    display: grid;
    place-items: center;
  }
  .thanh-tren__xh svg {
    width: 15px;
    height: 15px;
    fill: none;
    stroke: var(--trang);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
    opacity: 0.9;
  }
  .thanh-tren__xh a:hover svg {
    opacity: 1;
  }

  /* Tang 2 */
  .dau-trang__trong {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--k5);
    min-height: 68px;
  }

  .hieu {
    display: grid;
    gap: 1px;
    text-decoration: none;
    line-height: 1.1;
  }
  .hieu__ten {
    font-size: 20px;
    font-weight: var(--w-nang);
    letter-spacing: -0.01em;
    color: var(--do-500);
  }
  .hieu__phu {
    font-size: 10.5px;
    font-weight: var(--w-vua);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--muc-2);
  }

  .dieu-huong {
    display: flex;
    align-items: center;
    gap: var(--k5);
  }
  .dieu-huong__cap1 {
    display: flex;
    align-items: center;
    gap: var(--k5);
  }
  .dieu-huong__cap1 > li > a {
    display: flex;
    align-items: center;
    min-height: 68px;
    font-size: var(--cs-nho);
    font-weight: var(--w-dam);
    color: var(--muc);
    text-decoration: none;
    border-bottom: 2px solid transparent;
  }
  .dieu-huong__cap1 > li > a:hover,
  .dieu-huong__cap1 > li > a[aria-current='page'] {
    color: var(--do-500);
    border-bottom-color: var(--do-500);
  }

  /* ---------- mega menu ---------- */
  .mega {
    position: absolute;
    left: 0;
    right: 0;
    top: 100%;
    background: var(--trang);
    border-top: var(--vien-mong);
    border-bottom: 3px solid var(--do-500);
    box-shadow: var(--noi-lon);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-4px);
    transition:
      opacity var(--thoi) var(--nhip),
      transform var(--thoi) var(--nhip),
      visibility var(--thoi);
  }
  .co-mega:hover .mega,
  .co-mega:focus-within .mega {
    opacity: 1;
    visibility: visible;
    transform: none;
  }
  .mega__trong {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--k5) var(--gap-lon);
    padding-block: var(--k5);
  }
  .mega__dau {
    display: block;
    font-size: var(--cs-nho);
    font-weight: var(--w-nang);
    color: var(--muc);
    text-decoration: none;
    padding-bottom: var(--k2);
    margin-bottom: var(--k2);
    border-bottom: var(--vien-mong);
  }
  a.mega__dau:hover {
    color: var(--do-500);
  }
  .mega__ds {
    display: grid;
    gap: 3px;
  }
  .mega__ds a {
    display: block;
    padding: 3px 0;
    font-size: var(--cs-micro);
    font-weight: var(--w-thuong);
    color: var(--muc-2);
    text-decoration: none;
  }
  .mega__ds a:hover {
    color: var(--do-500);
  }
  .mega__mm {
    color: var(--muc-3);
    font-variant-numeric: tabular-nums;
  }
  .mega__mota {
    font-size: var(--cs-micro);
    color: var(--muc-2);
  }
  .mega__cot--nhan {
    background: var(--steel-050);
    margin: calc(var(--k5) * -1) 0;
    padding: var(--k5) var(--k4);
  }

  /* ---------- nut hamburger ---------- */
  .nut-menu {
    display: none;
    flex-direction: column;
    gap: 5px;
    padding: 12px;
    min-height: var(--cham);
    background: none;
    border: 0;
    cursor: pointer;
  }
  .nut-menu span {
    display: block;
    width: 24px;
    height: 2px;
    border-radius: 2px;
    background: var(--muc);
  }

  /* ---------- mobile ----------
     Giu nav day du. baogiathep an han nav duoi 900px - tuc cat het internal link
     o dung thiet bi chiem phan lon traffic. */
  @media (max-width: 980px) {
    .nut-menu {
      display: flex;
    }
    .dieu-huong {
      position: absolute;
      inset: 100% 0 auto 0;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      background: var(--trang);
      border-bottom: var(--vien-mong);
      box-shadow: var(--noi-lon);
      max-height: 0;
      padding: 0 20px;
      overflow: hidden auto;
      transition:
        max-height var(--thoi) var(--nhip),
        padding var(--thoi) var(--nhip);
    }
    .dieu-huong.mo {
      max-height: calc(100vh - 102px);
      padding: var(--k4) 20px var(--k5);
    }
    .dieu-huong__cap1 {
      flex-direction: column;
      align-items: stretch;
      gap: 0;
    }
    .dieu-huong__cap1 > li > a {
      min-height: var(--cham);
      border-bottom: var(--vien-mong);
    }
    .mega {
      position: static;
      opacity: 1;
      visibility: visible;
      transform: none;
      box-shadow: none;
      border: 0;
      border-bottom: var(--vien-mong);
    }
    .mega__trong {
      grid-template-columns: 1fr;
      padding-inline: 0;
      padding-block: var(--k4);
      gap: var(--k4);
    }
    .mega__cot--nhan {
      margin: 0;
      padding: var(--k4);
      border-radius: var(--bo-nho);
    }
    .dieu-huong__cta {
      margin-top: var(--k4);
    }
  }
</style>

<script>
  const nut = document.querySelector<HTMLButtonElement>('.nut-menu');
  const nav = document.getElementById('dieu-huong');
  nut?.addEventListener('click', () => {
    const mo = nav?.classList.toggle('mo');
    nut.setAttribute('aria-expanded', String(!!mo));
    nut.setAttribute('aria-label', mo ? 'Đóng menu' : 'Mở menu');
  });
</script>
